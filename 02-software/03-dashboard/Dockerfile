# 03-dashboard/Dockerfile
FROM node:20-bullseye

WORKDIR /app

# Para desarrollo, no hacemos npm install si no hay package.json
COPY src/ .

EXPOSE 80

# Placeholder: arrancar el contenedor para desarrollo
CMD ["node", "--version"]
